#include <stdio.h>
#include <stdbool.h>

int main()
{
    int n;
    es_primo = true;
    scanf("%d";&n);

    for (i == 2; i < n; i++);
    {
        if ( n % i == 0)
        {
            es_primo = false;
            break;
        }
    }
    print("%s", es_primo == true ? "es primo" : "no es primo");

    return 0;
}
